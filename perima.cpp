
//currently not in  use.
#include<cmath>
double perima(double x, double y, double z)
{
    return x + y + sqrt((pow(x,2), pow(y,2)));
}

//works but there is a bug. Presents in a rounded number rather than a decimal

